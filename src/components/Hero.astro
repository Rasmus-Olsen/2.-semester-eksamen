---
const { imgSrc } = Astro.props;
---


    <img class="hero" src={imgSrc} alt="">


<style>
     img.hero {
        position: relative;
        top: -80px;
        height: 90vh;
        object-fit: cover;
    }

    @media (max-width: 800px) {
        img.hero  {
            height: 60vh;
        }
    }
</style>